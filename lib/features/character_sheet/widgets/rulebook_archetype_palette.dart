import 'package:flutter/material.dart';

/// Printed rulebook colors for **archetype** chrome (purple band behind archetype
/// name on the build/archetype banner and badge labels that mark which archetype
/// a style/skill came from).
abstract final class RulebookArchetypePalette {
  /// Solid purple used for the archetype banner and matching name badges.
  static const Color band = Color(0xFF724073);
}
